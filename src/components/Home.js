const Home = (props) => {
	// const { msgAlert, user } = props
	console.log('props in home', props)

	return (
		<div className="container m-2">
			<h2>Who is Alina</h2>
		</div>
	)
}

export default Home
